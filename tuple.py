# is an ordered sequence of items, same as a list
# difference between tupple and list is that tuples are immutable
# we use () to create a tuple eg products=("Xbox",499.99, "Playstation",399.99)
# allows duplicates
product=('microsoft','Xbox','499.99')
print(product[0])
print(product[2])

language=('Java','Python','JavaScript','C+')
print(language)
print(language[0])
print(language[3])
print(len(language))
print(type(language))